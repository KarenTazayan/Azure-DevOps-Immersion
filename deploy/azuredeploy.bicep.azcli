# az bicep decompile --file azuredeploy.json
# Permanent deletion of soft delete enabled Azure Key Vault
# az keyvault purge --name kv-shopping-app-a1 --location germanywestcentral --no-wait --subscription xxx

az account set --subscription xxx
az group create --name rg-shopping-app --location "Germany West Central"
az deployment group create --resource-group rg-shopping-app `
    --template-file azuredeploy.bicep --parameters nameSuffix='a1'

